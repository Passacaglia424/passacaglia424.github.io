您还可以定义没有任何字段的结构！这些被称为 类似单元的结构，因为它们的行为类似于我们在“元组类型”部分()中提到的单元类型。当您需要在某些类型上实现特征但没有任何要存储在类型本身中的数据时，类似单元的结构可能很有用。
.as_mut_ptr()
如果你要使用一个特征的方法，那么你需要引入该特征到当前的作用域中

ErrorKind::NotFound 错误类型之一

.unwrap()

?符号简化错误处理

fs::read_to_string(File) 
打开一个文件、创建 String、读取文件内容最后写入字符串并返回

&self 其实是 self: &Self 的简写（注意大小写）。在一个 impl 块内，
Self 指代被实现方法的结构体类型，self 指代此类型的实例
self 表示 Rectangle 的所有权转移到该方法中，这种形式用的较少
&self 表示该方法对 Rectangle 的不可变借用
&mut self 表示可变借用

Rust 没有用 new 作为关键字
Box 可以将一个动态大小的特征变成一个具有固定大小的特征对象

#[derive(Copy)]
struct Foo;            为Foo实现Copy特征